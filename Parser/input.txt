boolean main(){
    remember decimal x = 5.0;
    remember number y = 5;
    checkif (x > 5.0) {
        tellme("x is greater than y");
    }
    otherwise {
        tellme("y is greater than x");
    }
    checktill (x >= 0.5) {
        tellme(x);
    }
    take("Enter a number: ", &y);
    remember letter z = 3.2 exp 4;
    return main;
}