number main(){
    remember number y = 5 * 4.2;
    remember decimal x = 10.7 * 2.0;
    checkif (x > 5.0) {
        tellme("x is greater than y");
    }
    otherwise {
        tellme("y is greater than x");
    }
    checktill (x >= 0.5) {
        tellme(x);
        x = x - 1;
    }
    take("Enter a number: ", &y);
    remember number z = 3 exp 4;
    return 0;
}